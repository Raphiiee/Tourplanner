<Window x:Class="Tourplanner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Tourplanner"
        xmlns:viewmodels="clr-namespace:Tourplanner.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:TourFolderVM}"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="600">

    <Grid Margin ="10">
        <StackPanel Orientation="Vertical" Grid.Row="1">
            <TextBlock> Media ITEM List</TextBlock>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
            
                <TextBlock Grid.Column="0" Text="Name:  " VerticalAlignment="Center"/>
                <TextBox   Grid.Column="1" VerticalAlignment="Center" Name="txtSearchName" Text="{Binding SearchName, Mode=TwoWay}"/>
                <Button    Grid.Column="2" Width="50" Margin="5,0,0,0" Command="{Binding SearchCommand}">Search</Button>
                <Button    Grid.Column="3" Width="50" Margin="5,0,0,0" Command="{Binding ClearCommand}">Clear</Button>
            </Grid>
        </StackPanel>
        <ListBox x:Name="listTourItems" ItemsSource="{Binding Items}" SelectedItem="{Binding CurrentItem}" DisplayMemberPath="Name" Margin="0,50,0,0"/>
    </Grid>


</Window>
